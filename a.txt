import pandas as pd

fname="C:/Users/student30/Downloads/Fläche_pandas.csv"
rfil=pd.read_csv(fname,sep=';')
print(rfil)
print(rfil.describe())

dfr=pd.DataFrame(rfil,columns=['Flaeche'])

print('Anzahl',len(dfr)['Flaeche'])
print('Mittelwert',dfr.mean()) 
print('Max',dfr.max())
print('Varianz',dfr.var())
print('Stabw.s',dfr.std())

print('Kovarianz\n',dfr.cov())
print('Korrelation\n',dfr.corr())

#print('Mittl. Fehler: ', dfr.var()['Flaeche']/math.sqrt(len(dfr)))
