from tkinter import *
import math
import matplotlib.pyplot as plt
import random

gui=Tk()
gui.geometry('350x350')
gui.title('Halllooo')
gui['bg']="Pink"

def ende():
    gui.destroy()
    
def berechnen():
    global v1, v2, v3, x, y
    v1= e_1.get()
    v2= e_2.get()
    v3= e_3.get()
    try:
        v1=float(v1)
        v2=float(v2)
        v3=float(v3)
        print(v1,v2,v3)
    except:
        print('Fehler')
    x = []
    y = []    
    vx= v1
    while vx <= v2:
        x.append(vx)
        y.append(math.exp(-2*vx))
        vx += v3
    for i in range(100000):
        px = random.random()*4+1 
        py = random.random()*math.exp(-2)
    

def diagramm():
    import matplotlib.pyplot as plt
    plt.plot(x, y)
    plt.grid()
    plt.show()
    
    
def export():
    filname ='C:/users/studentenoyumaa/desktop/test.txt'
    fileobj = open(filname,'w')
    for i in range(len(x)):
        hstr = (str(round(x[i], 3))+', '+str(round(y[i], 3))+'\n')
        fileobj.write(hstr)
    fileobj.close()
    

l_b = Label(gui, text='max')
l_b.place(x=10, y=30)

l_a = Label(gui, text='min')
l_a.place(x=10, y=60)

l_c= Label(gui, text ='step')
l_c.place(x=10, y=90)

e_b = Entry(gui)
e_a = Entry(gui)
e_c = Entry(gui)
e_d = Entry(gui)

e_b.place(x=50, y=30)
e_a.place(x=50, y=60)
e_c.place(x=50, y=90)


e_b.insert(0, 0)
e_a.insert(0, 10)
e_c.insert(0, 0.1)


b_b = Button(gui, text= 'Berechnen', command=berechnen)
b_b.place(x=10, y=130)
b_a = Button(gui, text= 'Diagramm', command=diagramm)
b_a.place(x=10, y=160)
b_d = Button(gui, text= 'Export', command=export)
b_d.place(x=10, y=190)
b_c = Button(gui, text= 'Beenden', command=ende, bg= 'red')
b_c.place(x=100, y=190)

gui.mainloop()
